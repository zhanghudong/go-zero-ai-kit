# Codex Rules

- Use ~/.goctl templates by default.
- Do not reverse-engineer templates from business code.
- Generators must be idempotent (skip by default, force overrides, dry_run previews).
- Return structured result JSON for all tools.
