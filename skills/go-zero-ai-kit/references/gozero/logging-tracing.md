# Logging & Tracing

适用场景：
- 补全 logic 日志
- 设计请求链路追踪字段
- 接入外部调用或 DB 埋点

核心规则：
- 统一日志字段为 `trace_id`、`span_id`、`request_id`。
- 请求入口处生成 `request_id` 并注入 `ctx`。
- 逻辑层使用结构化日志，不使用随意拼接字符串日志。
- 关键链路必须有埋点，至少覆盖 DB 操作与外部调用。

建议输出：
- 失败日志包含错误、业务主键、关键上下文字段。
- 外部调用日志包含目标系统、请求标识、耗时或结果摘要。
